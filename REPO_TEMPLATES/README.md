# Repository Templates

Reusable starting structures for new projects. Copy the template that best fits a project, then rename and tailor its documentation, source folders, and configuration.

## Available templates

- [`default/`](default/) — a language-agnostic baseline with a practical `.gitignore` and lightweight planning and task documentation.
